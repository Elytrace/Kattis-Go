#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector <int> arr;

    string command[] = {"push_back", "push_front", "push_middle", "get"};
    while(n--) {
        string com;
        int x;
        cin >> com >> x;
        if(com == command[0]) {
            
        }

    }

    return 0;
}