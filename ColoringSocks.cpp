#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main() {
    ll n, m, k;
    cin >> n >> m >> k;

    vector<ll> v(n);
    for(ll i : v) cin >> i;

    sort(v.begin(), v.end());

    ll machine_needs = 1;
    ll sock

    return 0;
}