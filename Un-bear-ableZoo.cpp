#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    while(cin >> n and n != 0) {
        string* list = new string[n];
        for(int i = 0; i < n; i++) {
            string s;
            getline(cin, s, ' ');
            char* strWord = strtok(s.data(), " ");
        }
    }

    return 0;
}